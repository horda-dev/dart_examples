name: twitter_client
description: "Horda client example. Twitter clone."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_auth: ^6.0.2
  firebase_core: ^4.1.0
  go_router: ^14.0.0
  horda_client: ^0.19.0
  logging: ^1.3.0
  image_picker: ^1.1.2
  twitter_server:
    hosted: https://pub.horda.ai/d368c1sgc98s738ue7cg
    version: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
